# Set the default line feed code to LF.
* text=auto eol=lf

# Linux Shell
*.sh    text eol=lf

# Windows Shell
*.bat   text eol=crlf
*.cmd   text eol=crlf
*.ps1   text eol=crlf

# Indicate all files that are completely binary and should not be changed.
*.png   binary
*.jpg   binary